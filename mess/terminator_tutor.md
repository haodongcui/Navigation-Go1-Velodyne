
# Install
```bash
sudo apt-get install terminator
```

# Config

Copy config from `terminator_config` at same folder

```bash
mv ~/.config/terminator/config ~/.config/terminator/config.bkp
vim ~/.config/terminator/config
```

ggVGd

i

ctrl+shift+v

:wq